# Инструкция по тестированию на новых данных

## 📁 Структура папки test_data/

Положите сюда новые тестовые данные. Поддерживаются два формата:

### Вариант 1: Готовый датасет (проще)
```
test_data/
  └── master_dataset.csv  (уже объединенные данные)
```

### Вариант 2: Отдельные файлы (как в тренировке)
```
test_data/
  ├── application_metadata.csv  (обязательно)
  ├── credit_hystory.csv        (опционально)
  ├── demographics.csv          (опционально)
  ├── financial_ratios.jsonl    (опционально)
  └── geographic_data.xml       (опционально)
```

## 🚀 Запуск тестирования

```bash
# Положите данные в test_data/
# Затем запустите:
python test_on_new_data.py
```

## 📊 Что получите

1. **Метрики качества** (если есть колонка `default`):
   - AUC-ROC
   - PR-AUC
   - F1-Score, F2-Score
   - Matthews Correlation
   - Confusion Matrix

2. **Файл с предсказаниями**: `test_data/predictions.csv`
   - `customer_id` или `application_id` (если есть)
   - `prediction` (0 или 1)
   - `probability` (вероятность дефолта)
   - `actual` (если была колонка `default`)

## 💡 Примечания

- Скрипт автоматически применяет **ту же обработку**, что и при обучении
- Использует **ту же обученную модель** (ансамбль)
- Работает даже если в новых данных **нет колонки `default`**
- Если каких-то колонок не хватает - создаст их с нулями

## ⚠️ Важно

Убедитесь что модель обучена:
- `models/best_model_final.pkl` (приоритет)
- или `models/best_model_ultimate.pkl`
- или `models/best_model_advanced.pkl`
